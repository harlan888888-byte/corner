name: Frontend CI/CD (Windows Server 2012 - 最终修复版)
on:
  push:
    branches: [ master ]
  workflow_dispatch:
    inputs:
      deploy_only:
        type: boolean
        description: "仅执行部署（跳过构建）"
        required: false
        default: false

jobs:
  build:
    if: ${{ !github.event.inputs.deploy_only }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - uses: pnpm/action-setup@v4
        with:
          version: latest
      - run: pnpm install
      - run: pnpm run build
      - uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: dist/
          retention-days: 7

  deploy:
    needs: build
    if: always() && (success() || github.event.inputs.deploy_only)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: frontend-dist
          path: dist/

      # ========== 核心修复：禁用自动打包解压，直接传输文件 ==========
      - name: Upload files to Windows Server 2012
        uses: appleboy/scp-action@v0.1.5
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASS }}
          port: 22
          source: "dist/**"
          target: "C:\\phpstudy_pro\\WWW\\vue_project\\temp_dist"
          overwrite: true
          strip_components: 1  # 去掉dist/前缀，直接把dist下的文件传到temp_dist
          rm: false  # 禁止删除目标目录，避免误操作

      # ========== 用PowerShell执行文件操作（保持不变） ==========
      - name: Execute file operations on server
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASS }}
          port: 22
          script: |
            C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe -Command "
              \$deployDir = 'C:\phpstudy_pro\WWW\vue_project\front'
              \$tempDir = 'C:\phpstudy_pro\WWW\vue_project\temp_dist'

              # 1. 确保目录存在
              if (-not (Test-Path \$deployDir)) { New-Item -ItemType Directory -Path \$deployDir -Force | Out-Null }
              if (-not (Test-Path \$tempDir)) { New-Item -ItemType Directory -Path \$tempDir -Force | Out-Null }

              # 2. 强制清空部署目录
              if (Test-Path \$deployDir) {
                Remove-Item -Path \$deployDir\* -Recurse -Force -ErrorAction SilentlyContinue
              }

              # 3. 强制移动文件
              Move-Item -Path \$tempDir\* -Destination \$deployDir -Force

              # 4. 删除临时目录
              Remove-Item -Path \$tempDir -Recurse -Force -ErrorAction SilentlyContinue

              # 5. 验证部署结果
              Write-Host '部署成功！最新文件修改时间：'
              Get-ChildItem -Path \$deployDir | Select-Object Name, LastWriteTime
            "