version: '1.0'
name: '2312312312312'
displayName: '1'
triggers:
  trigger: auto
  push:
    branches:
      prefix:
        - ''
stages:
  - name: stage-428839a4
    displayName: '12312312312'
    strategy: naturally
    trigger: auto
    executor: []
    steps:
      - step: execute@docker
        name: execute_by_docker
        displayName: 基于镜像的脚本执行
        certificate: ''
        image: docker.io
        command:
          - '# 请在此输入您想执行的脚本'
          - echo 'Hello Gitee!'
        artifacts:
          - name: BUILD_ARTIFACT
            path:
              - ./target
        notify: []
        strategy:
          retry: '0'
